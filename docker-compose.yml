version: '3.8'

services:
  pomodoro:
    build: .
    container_name: pomodoro-timer
    ports:
      - "${PORT:-3001}:3001"
    environment:
      - PORT=3001
    restart: unless-stopped
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true
